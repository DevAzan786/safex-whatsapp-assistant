# data/

- `safex_faq_dataset.json` — source FAQ dataset (place the file here).
- `chroma_db/` — persisted ChromaDB vector store (auto-generated on ingest,
  do not commit large binary index files if avoidable; add to .gitignore
  if size becomes an issue).
